version: "3.8"

services:
  patent-extractor:
    build: .
    container_name: patent-jsonl-extractor
    ports:
      - "8020:8020"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    environment:
      - MAX_FILE_SIZE=524288000
    restart: unless-stopped
